(module jazz.doc.catalog.Doc-Catalog jazz
  (import (jazz.doc))
  (class Doc-Catalog extends Component-Catalog
    (slot project getter generate)
    (method override (initialize self name client project)))
  (hub get-project))
